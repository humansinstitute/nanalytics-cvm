{
  "name": "cvm-alytics",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "start": "bun run server.ts",
    "clean-db": "bun run src/clean-db.ts",
    "type-check": "bun run tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@contextvm/sdk": "^0.1.40",
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.25.76"
  }
}
